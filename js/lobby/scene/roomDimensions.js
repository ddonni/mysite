// 방의 바닥/벽 치수 — 방 자체(roomShell.js)뿐 아니라 여러 가구가 "벽에
// 붙이기"/"바닥 가장자리 피하기" 계산에 이 값을 그대로 씀(예: 책장은
// 뒷벽 폭에 맞춰 짓고, 냉장고는 왼쪽 벽에, 공/곰인형은 바닥 경계 안에
// 머물러야 함) — 한 곳에서만 관리해야 방 크기를 바꿀 때 값이 안 어긋남.
export const FLOOR_W = 9;
export const FLOOR_D = 6.6;
export const WALL_H = 4.1;
